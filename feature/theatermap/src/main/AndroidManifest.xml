<manifest xmlns:dist="http://schemas.android.com/apk/distribution"
    package="soup.movie.theatermap">

    <dist:module
        dist:instant="false"
        dist:title="@string/map_label">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>
</manifest>
