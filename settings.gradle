include ':app'
include ':domain'
include ':testing'
include ':macrobenchmark'

file("feature").eachDir { include ":feature:${it.name}" }
file("data").eachDir { include ":data:${it.name}" }
file("core").eachDir { include ":core:${it.name}" }
